/*
** EPITECH PROJECT, 2019
** MUL_my_runner
** File description:
** portals_2.c
*/
#include <entitybase.h>

void my__portal(entity_t *self, entity_t *target)
{
    data_storage_t *data = get_data_storage();

    if (target->size == data->entities[0]->size) {
        self->health = 0;
        self->gravity = 4.f;
    }
}

void my__portal(entity_t *self, entity_t *target)
{
    data_storage_t *data = get_data_storage();

    if (target->size == data->entities[0]->size) {
        self->health = 0;
        self->gravity = 4.f;
    }
}

void my__portal(entity_t *self, entity_t *target)
{
    data_storage_t *data = get_data_storage();

    if (target->size == data->entities[0]->size) {
        self->health = 0;
        self->gravity = 4.f;
    }
}

void my__portal(entity_t *self, entity_t *target)
{
    data_storage_t *data = get_data_storage();

    if (target->size == data->entities[0]->size) {
        self->health = 0;
        self->gravity = 4.f;
    }
}

void my__portal(entity_t *self, entity_t *target)
{
    data_storage_t *data = get_data_storage();

    if (target->size == data->entities[0]->size) {
        self->health = 0;
        self->gravity = 4.f;
    }
}
